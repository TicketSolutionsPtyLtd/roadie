export const metadata = {
  title: 'Button',
  description: 'A fundamental building block for user interactions',
  status: 'beta',
  category: 'Actions'
}

import { Button } from '@oztix/roadie-components'
import { css } from '@oztix/roadie-core/css'

import { PropsDefinitions } from '@/components/PropsDefinitions'

# Button

The Button component is a fundamental building block for user interactions. It supports multiple appearances and sizes to accommodate different design contexts.

## Import

```tsx
import { Button } from '@oztix/roadie-components'
```

## Examples

### Appearances

Different button appearances for various contexts:

```tsx
live
<View gap='200' flexDirection='row'>
  <Button appearance='solid'>
    Solid Button
  </Button>
  <Button appearance='outline'>
    Outline Button
  </Button>
  <Button appearance='ghost'>
    Ghost Button
  </Button>
</View>
```

### Sizes

```tsx
live
<View gap='200' flexDirection='row' alignItems='center'>
  <Button size='sm'>Small</Button>
  <Button size='md'>Medium</Button>
  <Button size='lg'>Large</Button>
</View>
```

### Colors

Subtle is the default color for all buttons.

Use subtle for the majority of buttons. Use accent for the primary action in a set of buttons.

Use success, warning, and danger for specific situations. e.g. danger for destructive actions.

```tsx
live
<View gap='200'>
  <View flexDirection='row' gap='200'>
    <Button appearance='solid' color='subtle'>
      Subtle
    </Button>
    <Button appearance='solid' color='accent'>
      Accent
    </Button>
    <Button appearance='solid' color='inverse'>
      Inverse
    </Button>
    <Button appearance='solid' color='success'>
      Success
    </Button>
    <Button appearance='solid' color='warning'>
      Warning
    </Button>
    <Button appearance='solid' color='danger'>
      Danger
    </Button>
  </View>
  <View flexDirection='row' gap='200'>
    <Button appearance='outline' color='subtle'>
      Subtle
    </Button>
    <Button appearance='outline' color='accent'>
      Accent
    </Button>
    <Button appearance='outline' color='inverse'>
      Inverse
    </Button>
    <Button appearance='outline' color='success'>
      Success
    </Button>
    <Button appearance='outline' color='warning'>
      Warning
    </Button>
    <Button appearance='outline' color='danger'>
      Danger
    </Button>
  </View>
  <View flexDirection='row' gap='200'>
    <Button appearance='ghost' color='subtle'>
      Subtle
    </Button>
    <Button appearance='ghost' color='accent'>
      Accent
    </Button>
    <Button appearance='ghost' color='inverse'>
      Inverse
    </Button>
    <Button appearance='ghost' color='success'>
      Success
    </Button>
    <Button appearance='ghost' color='warning'>
      Warning
    </Button>
    <Button appearance='ghost' color='danger'>
      Danger
    </Button>
  </View>
</View>
```

## Usage

```tsx
live
<Button appearance='solid' size='md' onPress={() => alert('Button pressed')}>
  Click me
</Button>
```

<PropsDefinitions componentPath='packages/components/src/components/Button/index.tsx' />
