export const metadata = {
  title: 'Badge',
  description: 'A compact label for status, counts, or categories',
  status: 'beta',
  category: 'Content',
}

import { PropsDefinitions } from '@/components/PropsDefinitions'

# Badge

A compact label for status, counts, or categories.

## Import

```tsx
import { Badge } from '@oztix/roadie-components'
```

## Examples

### Default

```tsx-live
<Badge>Badge</Badge>
```

### Emphasis

```tsx-live
<div className='flex flex-row flex-wrap gap-2'>
  <Badge emphasis='strong'>Strong</Badge>
  <Badge emphasis='normal'>Normal</Badge>
  <Badge emphasis='subtle'>Subtle</Badge>
  <Badge emphasis='subtler'>Subtler</Badge>
</div>
```

### Sizes

```tsx-live
<div className='flex flex-row flex-wrap items-center gap-2'>
  <Badge size='sm'>Small</Badge>
  <Badge size='md'>Medium</Badge>
</div>
```

### Intents

```tsx-live
<div className='flex flex-row flex-wrap gap-2'>
  <Badge intent='neutral'>Neutral</Badge>
  <Badge intent='accent'>Accent</Badge>
  <Badge intent='brand'>Brand</Badge>
  <Badge intent='info'>Info</Badge>
  <Badge intent='success'>Success</Badge>
  <Badge intent='warning'>Warning</Badge>
  <Badge intent='danger'>Danger</Badge>
</div>
```

### With indicator

Use the `indicator` prop to show a dot before the text. Add `indicatorPulse` for a slow pulse animation.

```tsx-live
<div className='flex flex-row flex-wrap gap-2'>
  <Badge indicator intent='success'>Active</Badge>
  <Badge indicator indicatorPulse intent='danger'>Live</Badge>
  <Badge indicator intent='info'>Processing</Badge>
  <Badge indicator indicatorPulse intent='warning'>Attention</Badge>
</div>
```

### Hidden label

Use `hideLabel` to shrink the badge to just its dot. The badge's own surface is the dot, so `intent` and `emphasis` set its look, `size` sets its size, and `indicatorPulse` pulses it. The label stays in the DOM, visually hidden, so screen readers still announce it. Position the dot from its container; the badge has no placement prop.

```tsx-live
<div className='flex flex-row flex-wrap items-center gap-6'>
  <Badge hideLabel intent='danger' emphasis='strong'>3 unread</Badge>
  <Badge hideLabel indicatorPulse intent='success' emphasis='strong'>Live</Badge>
  <Badge hideLabel intent='accent' emphasis='subtle'>New</Badge>
  <Badge hideLabel size='sm' emphasis='normal'>Draft</Badge>
  <span className='relative inline-grid'>
    <IconButton aria-label='Notifications'>
      <BellRinging weight='bold' className='size-5' />
    </IconButton>
    <Badge hideLabel intent='danger' emphasis='strong' className='absolute end-1 top-1'>
      3 unread
    </Badge>
  </span>
</div>
```

### With icons

Place icons directly as children. SVGs are automatically sized to match the text.

```tsx-live
<div className='flex flex-row flex-wrap gap-2'>
  <Badge intent='success'><CheckCircle weight='bold' /> Verified</Badge>
  <Badge intent='danger'><XCircle weight='bold' /> Failed</Badge>
  <Badge intent='info'><Info weight='bold' /> Info</Badge>
  <Badge intent='warning'><Warning weight='bold' /> Caution</Badge>
</div>
```

## Guidelines

Use `hideLabel` only where space is tight and the surrounding context explains the dot, like the corner of a collapsed nav tile. The hidden label is what a screen reader hears, so it must still describe the state, such as "3 unread", not "3".

<PropsDefinitions componentPath='packages/components/src/components/Badge/index.tsx' />
