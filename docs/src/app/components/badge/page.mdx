export const metadata = {
  title: 'Badge',
  description: 'A small label for status, counts, or categorical information',
  status: 'beta',
  category: 'Data Display',
}

import { PropsDefinitions } from '@/components/PropsDefinitions'

# Badge

The Badge component is a compact label used to display status, counts, or
categorical information. It supports intents, emphasis levels, sizes,
icons, and animated indicators.

## Import

```tsx
import { Badge } from '@oztix/roadie-components'
```

## Examples

### Emphasis

```tsx-live
<div className='flex flex-row flex-wrap gap-2'>
  <Badge emphasis='strong'>Strong</Badge>
  <Badge emphasis='default'>Default</Badge>
  <Badge emphasis='subtle'>Subtle</Badge>
  <Badge emphasis='subtler'>Subtler</Badge>
</div>
```

### Intents

```tsx-live
<div className='flex flex-row flex-wrap gap-2'>
  <Badge intent='neutral'>Neutral</Badge>
  <Badge intent='accent'>Accent</Badge>
  <Badge intent='brand'>Brand</Badge>
  <Badge intent='info'>Info</Badge>
  <Badge intent='success'>Success</Badge>
  <Badge intent='warning'>Warning</Badge>
  <Badge intent='danger'>Danger</Badge>
</div>
```

### Sizes

```tsx-live
<div className='flex flex-row flex-wrap items-center gap-2'>
  <Badge size='sm'>Small</Badge>
  <Badge size='md'>Medium</Badge>
</div>
```

### With indicator

Use the `indicator` prop to show a dot before the text. Add `indicatorPulse` for a slow pulse animation.

```tsx-live
<div className='flex flex-row flex-wrap gap-2'>
  <Badge indicator intent='success'>Active</Badge>
  <Badge indicator indicatorPulse intent='danger'>Live</Badge>
  <Badge indicator intent='info'>Processing</Badge>
  <Badge indicator indicatorPulse intent='warning'>Attention</Badge>
</div>
```

### With icon

Pass any React element as the `icon` prop. Use Phosphor Bold icons for consistency.

```tsx-live
<div className='flex flex-row flex-wrap gap-2'>
  <Badge intent='success' icon={<CheckCircle weight='bold' />}>Verified</Badge>
  <Badge intent='danger' icon={<XCircle weight='bold' />}>Failed</Badge>
  <Badge intent='info' icon={<Info weight='bold' />}>Info</Badge>
  <Badge intent='warning' icon={<Warning weight='bold' />}>Caution</Badge>
</div>
```

### Combined

```tsx-live
<div className='flex flex-row flex-wrap gap-2'>
  <Badge emphasis='strong' intent='success' indicator icon={<CheckCircle weight='bold' />}>Published</Badge>
  <Badge emphasis='strong' intent='danger' indicator indicatorPulse>Urgent</Badge>
  <Badge emphasis='subtle' intent='accent' icon={<Star weight='bold' />}>Featured</Badge>
  <Badge emphasis='subtler' intent='neutral' size='md'>Draft</Badge>
</div>
```

<PropsDefinitions componentPath='packages/components/src/components/Badge/index.tsx' />
