/**
 * @deprecated Import from `@oztix/roadie-widgets/cart` instead.
 *
 * The shared cart core moved to `cart` — it's used by both the cart-drawer and
 * cart-contents widgets, so it no longer lives under cart-drawer. This is a
 * back-compat re-export so existing `@oztix/roadie-widgets/cart-drawer/core`
 * consumers keep working; it will be removed in the next major.
 */
export * from '../../cart'
