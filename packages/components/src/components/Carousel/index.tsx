'use client'

import type { ComponentProps } from 'react'

/**
 * Compound horizontal or vertical carousel built on Embla Carousel v9.
 *
 * This is a stub — Phase 1 scaffolding only. The full compound API lands
 * in Phase 2 (core primitives) and Phase 3 (autoplay + a11y).
 */
function CarouselRoot(_props: ComponentProps<'div'>) {
  return null
}

CarouselRoot.displayName = 'Carousel'

export const Carousel = CarouselRoot

export type CarouselProps = ComponentProps<'div'>
