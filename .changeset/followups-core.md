---
'@oztix/roadie-core': patch
---

- `duration-*` utilities set `--tw-duration`, so a variant-scoped `transition-[…]` keeps their duration.
- `cn` merges `emphasis-field` with the other emphasis presets.
