---
'@oztix/roadie-core': patch
---

- Named `duration-*` utilities set `--tw-duration`, so a variant-scoped `transition-[…]` keeps their duration, and they now ship in `roadie.compiled.css`.
- `cn` merges `emphasis-field` with the other emphasis presets.
