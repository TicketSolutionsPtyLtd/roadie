---
"@oztix/roadie-core": minor
---

Add the CSS the application frame needs: `--navigator-rail-compact` /
`--navigator-rail-nested` rail-width tokens, the `motion-drawer` utility that
drives a drawer's edge transition and tracks Base UI's live swipe offset, and
`animate-pop-tap` — a 200ms tap response, where `animate-pop` at 600ms reads as
a notification.
